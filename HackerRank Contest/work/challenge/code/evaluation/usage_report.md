# Usage report

Final full-dataset run: deterministic local Python agent (`code/main.py`).

| Provider / model | Calls | Input tokens | Output tokens | Estimated cost |
|---|---:|---:|---:|---:|
| None — local deterministic forecast | 0 | 0 | 0 | $0.00 |

The agent uses CSV evidence, fixed exchange rates, local optional OCR, a 90-day
cash-flow forecast, and deterministic plan verification. It makes no external
model/API calls; therefore the total and average token use per request are both
zero and estimated model cost is $0.00.
